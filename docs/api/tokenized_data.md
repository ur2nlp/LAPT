# `lapt.tokenized_data`

Turn corpus text into the tokenized datasets training consumes — the stateless
transformations and the cached artifacts that run them.

A peer of `lapt.sources`, which is the untokenized layer above it.

::: lapt.tokenized_data
