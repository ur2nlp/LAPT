# `lapt_core.artifacts`

Content-tracked caching for multi-stage training pipelines. This module is
framework-general: it depends only on the standard library and PyYAML, and is
shared with sibling projects rather than reimplemented in each.

::: lapt_core.artifacts
