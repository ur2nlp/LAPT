# `lapt_core.dataset_artifacts`

A `CachedArtifact` specialization for corpora stored as HuggingFace datasets,
plus the registry that maps a source configuration's `type` field to a concrete
class.

::: lapt_core.dataset_artifacts
