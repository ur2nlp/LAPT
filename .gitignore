data/
models/
outputs/
# Local one-off shell wrappers. fetch_results.sh is excepted: the README
# documents it as the way to pull cluster results, so a fork needs it.
scripts/*
!scripts/fetch_results.sh
tokenizers/
experiments/
plots/
presentations/
# fetched cluster job output (scripts/fetch_results.sh)
generation

.DS_Store
.claude
.pytest_cache
.vscode
__pycache__

TODO.md

# Workflow-specific reference files
num_examples.txt
tools/jsonl_to_spreadsheet.py
tools/visualize_seed_vocab_concept.py

# Python build/tooling artifacts
.ruff_cache
*.egg-info/
build/
dist/

# Coverage data
.coverage
.coverage.*
htmlcov/

# Mutation testing working tree (mutmut copies the project here)
mutants/
.mutmut-cache

# mkdocs build output
site/
